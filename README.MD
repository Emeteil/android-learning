# Android learning 
Воробьёв Дмитрий Андреевич ИКС-433

---

# 1. Основы ООП. “Ходячий”

## Основные сущности(классы)

### Точка в пространстве
```kotlin
class Point(_x: Float, _y: Float)
```
- Координаты: `(x, y) ∈ ℝ²`
- Преобразование: `P' = P + V × multiplier`

### Нормализованный вектор
```kotlin
class Vector2(_x: Float, _y: Float)
```
- Нормализация: `V = (x/‖V‖, y/‖V‖)`, где `‖V‖ = √(x² + y²)`
- Компоненты: `x, y ∈ [-1, 1]`

### Человек (движущийся объект)
```kotlin
class Human(fullName: String, age: Int, groupNumber: Int, startSpeed: Float)
```

## Математическая модель движения

### Генерация случайного направления
```kotlin
fun RandomizeMoveVector()
```
- `Vₓ = random(-1, 1)`
- `Vᵧ = random(-1, 1)`
- Вектор нормализуется: `V̂ = V/‖V‖`

### Движение
```kotlin
fun Move()
```
- `Pₜ₊₁ = Pₜ + V̂ × speed`
- Где `speed` - постоянная скорость движения

## Алгоритм симуляции
1. Инициализация объектов с начальными позициями
2. Цикл движения:
   - Случайная генерация вектора направления
   - Пересчет позиции: `P ← P + V̂ × speed`
   - Отображение текущего состояния
```kotlin
while (true) {
    people.RandomizeMoveVector()
    people.Move()
    people.Print()
}
```
*Для единиц измерений можно выбрать любые значения, например метры и секунды, тогда 1 итерация -> 1 секунда. Если понадобится можно переключиться на другие единицы измерения без изменения основной логики движения(допустим 1 итерация - 1 миллисекунда(одна тысячная секунды)). В коде используются условные единицы, которые можно интерпритировать как угодно в будуйщем*

---

# 2. Наследование

## Реализация наследования в системе движения объектов

### Базовый класс Human
```kotlin
open class Human(fullName: String, age: Int, groupNumber: Int, startSpeed: Float)
```

### Класс-наследник Driver
```kotlin
class Driver(name: String, startSpeed: Float, moveVector2: Vector2) : Human(name, 0, 0, startSpeed)
```

## Ключевые особенности реализации

### 1. Наследование свойств
- **Driver** наследует от **Human**:
  - Позицию `currentPosition`
  - Скорость `currentSpeed` 
  - Вектор движения `currentMoveVector2`
  - Методы движения и отображения

### 2. Прямолинейное движение (переопределение метода)
```kotlin
override fun RandomizeMoveVector() {} // движение прямолинейно - рандомизация не нужна
```
- Водитель движется по заданному вектору без случайных изменений направления
- Начальный вектор движения задается при создании объекта

### 3. Многопоточная симуляция
```kotlin
val THREAD_COUNT = 4
val threads: MutableList<Thread> = mutableListOf<Thread>()
```
- Создается несколько потоков для параллельного движения объектов
- Каждый поток обрабатывает подмножество объектов с шагом `THREAD_COUNT`

### 4. Потокобезопасность
```kotlin
val mutex: ReentrantLock = ReentrantLock()
```
- Все классы (`Point`, `Vector2`, `Human`) реализованы как потокобезопасные
- Используются мьютексы `ReentrantLock` для синхронизации доступа к общим ресурсам

## Алгоритм работы системы

### Инициализация объектов
```kotlin
var peoples: Array<Human> = arrayOf(
    Human("Я", 19, 433, 1f),
    Human("Петя", 20, 123, 1f), 
    Human("Ваня", 20, 867, 1f),
    Driver("Таксист Олег", 2f, Vector2(1f, 0f)) // машина едет в 2 раза быстрее по оси X
)
```

### Параллельное движение
1. **Создание потоков**: `THREAD_COUNT` потоков для обработки объектов
2. **Распределение объектов**: каждый поток обрабатывает объекты с шагом `THREAD_COUNT`
3. **Цикл движения**:
   - Генерация вектора движения (для Human - случайный, для Driver - постоянный)
   - Пересчет позиции: `Move()`
   - Отображение состояния: `Print()`
4. **Управление потоками**: остановка по команде пользователя

### Особенности движения Driver
- **Скорость**: в 2 раза выше обычного человека
- **Направление**: строго по оси X (`Vector2(1f, 0f)`)
- **Визуализация**: отображается эмодзи 🛻 вместо 🏃

## Математическая модель

### Движение Driver  
- Постоянный вектор: задается при создании
- Позиция: `Pₜ₊₁ = Pₜ + V₀ × speed`
- Где `V₀` - начальный нормализованный вектор

*Также разделил код на несколько файлов, для удобства*

---

# 3. Интерфейсы

## Введение интерфейса Movable

### Базовый интерфейс для движущихся объектов
```kotlin
interface Movable
{
    val currentPosition: Point
    val currentMoveVector2: Vector2
    val currentSpeed: Float

    fun RandomizeMoveVector()
    fun Move()
    fun Print(iteration: Int? = null)
}
```

## Ключевые изменения в архитектуре

### 1. Универсальная коллекция объектов
```kotlin
var peoples: MutableList<Movable> = mutableListOf(
    Human("Я", 19, 433, 1f),
    Human("Петя", 20, 123, 1f),
    Human("Ваня", 20, 867, 1f),
    Driver("Таксист Олег", 2f, Vector2(1f, 0f))
)
```
- **Array<Human> → MutableList<Movable>** - универсальный список для любых Movable объектов
- Система теперь поддерживает любые типы движущихся объектов, реализующих интерфейс Movable

### 2. Реализация интерфейса в классах
```kotlin
open class Human(fullName: String, age: Int, groupNumber: Int, startSpeed: Float) : Movable
```
- Класс Human теперь явно реализует интерфейс Movable
- Свойства движения помечены как `override`

### 3. Архитектурное решение наследования
```kotlin
class Driver(name: String, startSpeed: Float, moveVector2: Vector2) : Human(name, 0, 0, startSpeed)
```
- **Driver остался наследником Human**, хотя мог быть независимой реализацией Movable
- Сохранена логическая связь "Водитель → Человек" для повторного использования функциональности

### 4. Многофайловая структура
```
src/main/kotlin/
├── Main.kt
├── Movable.kt # Интерфейс Movable
├── Human.kt
├── Driver.kt
├── Point.kt
└── Vector2.kt
```
- Добавлен новый файл Movable.kt с интерфейсом Movable

*Так как разделение остальной логики на файлы было сделалано в прошлой лабораторной, в этой особых изменений не произошло*

---

# 4. Android. Разработка простейшего калькулятора

## Архитектура приложения

### Основные компоненты
- **MainActivity** - главная активность, управляющая UI
- **CalculatorEngine** - вычислительный движок (алгоритм сортировочной станции)
- **Custom Views** - кастомные стили для кнопок и отображения

## Ключевые особенности реализации

### 1. Пользовательский интерфейс
```xml
<GridLayout
    android:id="@+id/gridLayout"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1"
    android:columnCount="4"
    android:rowCount="7">
```
- **7 строк × 4 колонки**
- **Категории кнопок**:
  - Цифры (0-9) и точка
  - Базовые операции (+, -, ×, ÷, ^)
  - Математические функции (sin, cos, log, pi)
  - Скобки и управление (C, =)

### 2. Визуальное оформление
```xml
<style name="CalcButton" parent="Widget.AppCompat.Button">
    <item name="android:layout_width">0dp</item>
    <item name="android:layout_height">0dp</item>
    <item name="android:layout_margin">4dp</item>
    <item name="android:textSize">26sp</item>
    <item name="android:textStyle">bold</item>
</style>
```
- **Цветовая схема**: темная тема
- **Закругленные углы**: 16dp
- **Разные цвета** для различных типов кнопок

### 3. Логика вычислений (CalculatorEngine)

#### Токенизация выражения
```kotlin
private fun Tokenize(expression: String): List<String>
```
- Разделение на числа, операторы, функции и константы
- Обработка унарных операторов (+/-)
- Валидация корректности токенов

#### Алгоритм сортировочной станции ([Shunting Yard](https://en.wikipedia.org/wiki/Shunting_yard_algorithm))
```kotlin
private fun ShuntingYard(tokens: List<String>): List<String>
```
- Создание RPN
- Учет приоритетов операторов и скобок
- Обработка математических функций
*[Реализация тут](https://habr.com/ru/articles/273253)*

#### Вычисление RPN
```kotlin
private fun EvaluateRPN(rpn: List<String>): Double
```
- Стековая машина для вычислений
- Поддержка унарных и бинарных операторов
- Обработка математических функций и констант

### 4. Обработка пользовательского ввода

#### Валидация выражений
```kotlin
private fun AddToken(token: String)
```
- Проверка корректности десятичных точек
- Валидация последовательности операторов
- Контроль парности скобок
- Ограничение длины выражения (15 символов)

#### Особые случаи
- **Унарные минусы**: автоматическое преобразование
- **Константы**: замена на числовые значения (pi)
- **Функции**: правильная обработка аргументов

### 5. Обработка ошибок
```kotlin
try
{
    result = calculatorEngine.CalculateExpression(currentExpression)
}
catch (e: Exception)
{
    UpdateInputField("Wrong")
    currentExpression = ""
    return
}
```
- **Try-catch блоки** для обработки вычислительных ошибок
- **Понятные сообщения** пользователю ("Wrong")
- **Автоочистка** некорректных выражений

## Математические возможности

### Поддерживаемые операции
- **Арифметические**: +, -, ×, ÷, ^ (степень)
- **Тригонометрические**: sin, cos
- **Логарифмические**: log (десятичный)
- **Константы**: π

### Особенности вычислений
- **Приоритет операторов**: ^ > ×÷ > +-
- **Поддержка скобок**: изменение порядка вычислений
- **Точность**: округление до 15 знаков после запятой (для избегания ситуации 0.1 + 0.2 = 0.30000...)

### Ограничения
- **Длина выражения**: максимум 15 символов
- **Портретная ориентация**: фиксированная

---